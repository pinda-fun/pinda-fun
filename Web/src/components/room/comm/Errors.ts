export enum CommError {
  NoMorePin = 'NoMorePin',
  Timeout = 'Timeout',
  Other = 'Other',
}

export enum PushError {
  NoChannel = 'NoChannel',
  Timeout = 'Timeout',
  Unauthorised = 'Unauthorised',
  Other = 'Other',
}
